/**
 * mul - multiplies two integers.
 * @a: accepts char equivalent of int using ASCII
 * @b: accepts char equivalent of int using ASCII
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
